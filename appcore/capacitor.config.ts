import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thomaschampagne.elevate',
  appName: 'elevate-sports-app',
  webDir: '../dist/app/'
};

export default config;
